export interface Player {
    id: string,
    name: string,
    matches?: string[],
    goals?: number[],
    medium?: number[],
    shooter?: number[],
    center?: number[],
    defense?: number[]
}
